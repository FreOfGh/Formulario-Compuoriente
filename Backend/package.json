{
  "name": "backend-compuoriente",
  "version": "1.0.0",
  "description": "Sistema de inscripciones para el Politécnico CompuOriente",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
  "postinstall": "npx playwright install chromium && apt-get update && apt-get install -y libglib2.0-0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "express",
    "playwright",
    "railway"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.12",
    "pdf-lib": "^1.17.1",
    "pg": "^8.17.1",
    "playwright": "^1.57.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}